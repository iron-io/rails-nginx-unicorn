machine:
  services:
    - docker
checkout:
  post:
    - echo '{"https://index.docker.io/v1/":{"auth":"$DOCKER_AUTH","email":"$DOCKER_EMAIL"}}' > $HOME/.dockercfg

dependencies:
  pre:
    - go get github.com/noqcks/gucci

test:
  override:
    - ./build.sh
